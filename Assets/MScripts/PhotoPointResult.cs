namespace MonaLisaGame
{
    public enum PhotoPointState
    {
        Unfocused,
        Focused,
    }

    public enum PhotoPointResult
    {
        Succeeded,
        Failed,
        Cancelled,
    }
}
